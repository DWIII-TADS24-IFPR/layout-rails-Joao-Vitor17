<% content_for :title, "New post" %>

<div class="container mx-auto px-4 py-8">
  <h1 class="text-4xl font-extrabold text-gray-900 mb-8">Create a New Post</h1>

  <%= render "form", post: @post %>

  <div class="mt-6">
    <%= link_to "Back to posts", posts_path, class: "text-blue-600 hover:text-blue-800 text-lg font-semibold" %>
  </div>
</div>
